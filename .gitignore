node_modules
.next
out
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store

# Tiptap Pro registry auth
# .npmrc is committed but must only reference the token via ${TIPTAP_PRO_TOKEN}.
# Never hardcode the actual token here.

# Local build and visual QA artifacts
tsconfig.tsbuildinfo
tmp/
.npm-cache/
